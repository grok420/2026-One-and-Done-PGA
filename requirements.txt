# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Web UI (Streamlit)
streamlit>=1.28.0
plotly>=5.18.0

# Machine learning (for simulations)
scikit-learn>=1.3.0

# CLI (optional - comment out for cloud deployment if issues)
click>=8.0.0
rich>=13.0.0

# Note: The following are for local development only
# Install separately with: pip install selenium webdriver-manager beautifulsoup4 pyttsx3 pytest
